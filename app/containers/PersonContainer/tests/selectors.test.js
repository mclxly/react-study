import {
  selectPersonContainer,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectPersonContainer();

describe('selectPersonContainer', () => {

});
