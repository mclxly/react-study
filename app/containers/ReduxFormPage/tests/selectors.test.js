import {
  selectReduxFormPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectReduxFormPage();

describe('selectReduxFormPage', () => {

});
